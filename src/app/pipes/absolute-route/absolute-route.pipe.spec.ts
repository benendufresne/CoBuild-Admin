import { AbsoluteRoutePipe } from './absolute-route.pipe';

describe('AbsoluteRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new AbsoluteRoutePipe();
    expect(pipe).toBeTruthy();
  });
});

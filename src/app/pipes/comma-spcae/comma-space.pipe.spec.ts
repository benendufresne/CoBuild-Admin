import { CommaSpacePipe } from './comma-space.pipe';

describe('CommaSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new CommaSpacePipe();
    expect(pipe).toBeTruthy();
  });
});

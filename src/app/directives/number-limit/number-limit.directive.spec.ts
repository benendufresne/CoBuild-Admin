import { NumberLimitDirective } from './number-limit.directive';

describe('NumberLimitDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberLimitDirective();
    expect(directive).toBeTruthy();
  });
});

import { FirstLetterCapsDirective } from './first-letter-caps.directive';

describe('FirstLetterCapsDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstLetterCapsDirective();
    expect(directive).toBeTruthy();
  });
});

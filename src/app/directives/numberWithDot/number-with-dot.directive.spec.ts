import { NumberWithDotDirective } from './number-with-dot.directive';

describe('NumberWithDotDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberWithDotDirective();
    expect(directive).toBeTruthy();
  });
});
